# CHANGELOG — рішення й контекст, які не видно з коду одразу

Онови цей файл (і в GitHub, і в Project Knowledge), коли ухвалюєш
щось важливе поза межами звичайного "додав урок". Мета — щоб новий
чат у проєкті міг прочитати цей файл і зрозуміти контекст за хвилину,
а не гортати історію чатів.

## Дизайн
- Обрано "зошитовий" стиль (теплий папір/бірюза/теракота, компас,
  прогрес-літак) — два інші варіанти (підручниковий, грайливий)
  відхилені після порівняння прев'ю.

## Навчальна програма
- Теми модулів беруться з офіційного календарно-тематичного
  планування (PDF за 7/8/9 класи), не вигадуються довільно.
- **9 клас має лише 7 модулів**, не 8 — за програмою тем сім, восьма
  клітинка (`modules-data.js` → `H`) навмисно порожня.
- **7 клас, Модуль A** — офіційна тема "Я, моя родина, мої друзі";
  Урок 1 у цьому модулі — стандартний вступний урок-вікторина про
  Німеччину (спільний для кількох класів на початку року), а не
  контент про сімейні канікули.

## Структурні механізми
- Тема МОДУЛЯ живе окремо від уроків (`modules-data.js`), не в кожному
  уроці — можна проставити наперед, до появи самих уроків.
- Мітка "нове" — без дати. Показується на уроці, що стоїть ОСТАННІМ
  у масиві `LESSONS` для свого класу — тому нові записи завжди
  дописуються в кінець масиву, ніколи не вставляються в середину.
- Перевірка ДЗ — через поле `dz` в `lessons-data.js`: наступний урок
  модуля бере `dz` попереднього для вкладки "Hausaufgabe".
- Навігація (хедер + хлібні крихти) — один спільний `site-nav.js`,
  не дублюється по сторінках.

## Технічне
- Хостинг: GitHub Pages, файл `.nojekyll` у корені обов'язковий
  (без нього Jekyll іноді підміняє сайт дефолтним README).
- При завантаженні на GitHub — перетягувати ВМІСТ папки, а не саму
  папку (інакше все лягає на рівень глибше).

## Анти-чіт (Hausaufgabenkontrolle / Üb. з пропусками)
- Проблема: `checkBlanks` при неправильній відповіді сама дописувала
  текст "правильно: ..." біля поля — учень міг скопіювати цей текст
  назад у поле й одразу отримати залік.
- Рішення (2026-09): текстову підказку прибрано повністю, лишився
  тільки колір рамки (correct/wrong), без розкриття самої відповіді.
  Додатково бал і прогрес тепер фіксуються по ПЕРШІЙ непорожній спробі
  кожного поля (`checkBlanks._locked`) — подальші зміни поля так само
  підсвічуються живо, але вже не впливають на рахунок і прогрес-смугу.
  Кнопка "Zurücksetzen" (`resetBlanks`) знімає й цю заморозку — чесний
  новий старт.
- Зміна лише в спільному `assets/lesson-interactions.js`, окремі уроки
  не чіпались і не потребують правок.
- `buildQuiz`/`checkTokens` не чіпались: квіз блокує повторний клік по
  питанню, токени й так не розкривають відповідь текстом — там цього
  експлойту не було.

## Анти-чіт: заборона вставки в поля для письма (2026-09)
- Проблема: у письмових вправах (`textarea`) учні вставляли готовий текст,
  скопійований з інтернету, перекладача чи чату з ШІ.
- Рішення: у спільному `assets/lesson-interactions.js` додано
  `guardNoPaste(el)` / `initNoPaste()` — автоматично для ВСІХ `textarea`
  (опт-аут атрибутом `data-allow-paste`). Блокуються paste, drop і
  `beforeinput` з `insertFromPaste/Drop/Yank`; запасний варіант для
  мобільних клавіатур — відкат, якщо за одну подію `input` додалося
  понад 30 символів. Учень бачить повідомлення під полем. Копіювати свій
  текст З поля можна.
- Старі уроки з `textarea` отримують захист автоматично після заміни
  файлу на сайті — самі уроки правити не треба.
- Обмеження: не захищає від передруку вручну, вимкненого JS чи
  інструментів розробника; голосовий набір довгими фразами теж
  відкочується.
- Заодно в STYLE-BRIEF.md виправлено застарілу фразу, ніби `checkBlanks`
  показує правильну відповідь (її прибрано ще попереднім анти-чіт фіксом).

## CSS-баги (виправлено в спільному lesson-style.css)
- **Відсутній пробіл між німецьким словом і перекладом у `.ua-note dl`**
  (2026-09): `dt` (німецьке слово) і `dd` (переклад) обидва мали
  `display:inline` без жодного роздільника між ними, тому текст
  зливався в одне слово, напр. „Viertel vor achtза чверть восьма”.
  Виправлено додаванням `.ua-note dt::after{content:" — ";}` — тепер
  між `dt` і `dd` завжди рендериться „ — ” незалежно від того, що
  саме вписано в конкретному уроці.
- Зміна лише в спільному `assets/lesson-style.css` (за прикладом
  анти-чіт фіксу нижче) — самі уроки, які вже використовують патерн
  `<dl><dt>...</dt><dd>...</dd></dl>` всередині `.ua-note`, НЕ
  потребують правок: після заміни файлу на сайті вигляд виправиться
  автоматично в усіх уроках одразу.
- **Це системний клас проблем, не одноразова помилка**: перш ніж
  писати `<dl>`/`dt`/`dd` (чи будь-яку іншу розмітку) всередині
  `.ua-note` для нового уроку, звіряй її з тим, як реально рендерить
  сучасний `lesson-style.css` — а не лише з тим, як виглядає розмітка
  в HTML. Якщо після генерації уроку в розмітці `.ua-note` є щось,
  чого раніше не було (нова комбінація тегів, новий клас), варто
  подумки „прогнати” її крізь актуальний CSS перед тим, як віддавати
  готовий файл.

## Каталог уроків: згортальні модулі (2026-09)
- Проблема: у 7 класі Модуль A розрісся до 18 уроків, і до модуля B
  (де скоро з'являться уроки) треба було довго гортати.
- Рішення (лише `assets/site.js` + `assets/site.css`, HTML сторінки
  `klasy/` не мінявся):
  - Модулі з уроками рендеряться як `<details>`. Відкритий за
    замовчуванням лише **актуальний** модуль = модуль уроку з міткою
    "нове". Тобто це те саме правило "останній у масиві `LESSONS` для
    класу" — окремого поля чи налаштування "поточний модуль" НЕМАЄ.
    Наслідок: порядок дописування в кінець масиву тепер впливає ще й
    на те, який модуль розгорнутий.
  - Порожні модулі — компактний рядок "ще немає уроків" (не
    `<details>`, розгортати нічого). Правило "видно всі 8 модулів"
    збережено.
  - Липка панель `.module-jump` над модулями: чип на кожен модуль з
    кількістю уроків; клік розгортає модуль і прокручує до нього.
    Поточний модуль залитий кольором, порожні — приглушені.
  - Під час пошуку всі знайдені модулі розгорнуті, панелі немає.
  - Стан згортання НЕ запам'ятовується (без localStorage) — при
    кожному відкритті сторінки знову розгорнутий актуальний модуль.
    Якщо захочеться пам'ятати — додати пізніше, ключуючи стан за
    актуальним модулем, щоб новий модуль усе одно відкривався сам.
- Заодно виправлено відмінювання лічильника: було "2 уроків", тепер
  `lessonCountLabel()` — 1 урок / 2–4 уроки / 5+ уроків / 21 урок.

## Глосарій
- Сторінка `glosariy/` тепер сортує слова за абеткою (замість
  порядку додавання/уроків) і показує буквений покажчик угорі для
  швидкого переходу (клік по літері → скрол до групи слів на неї).
  Заголовки-літери над групами "прилипають" зверху при скролі.
- Сортування рахується за окремим ключем (`glossarySortKey` в
  `site.js`), який ігнорує артикль (`der`/`die`/`das`) і `sich` на
  початку слова — інакше майже всі іменники опинилися б під літерою
  "Д". Сам текст слова (`de`) в `glossary-data.js` не змінюється,
  ключ рахується на льоту при рендері.
- Фільтри за класом/модулем і пошук лишились без змін — сортування
  за абеткою застосовується ДО вже відфільтрованого списку.
- У вкладці "Fertig!" кожного уроку тепер є картка "Wörter zum Lernen"
  зі словами саме цього уроку — щоб учневі було зрозуміло, що́ вчити
  після кожного окремого уроку, а не лише в цілому за модуль/клас.
  Це НЕ новий масив даних: `lesson-template.html` підключає той самий
  `data/glossary-data.js` і сам фільтрує `GLOSSARY` за трьома
  константами (`LEKTION_KLAS`/`LEKTION_MODUL`/`LEKTION_LEKTION`), які
  виставляються при копіюванні шаблону й мають збігатися з тегами
  цього уроку в `lessons-data.js`. Слова показуються парою `de — uk`.
  Картка розміщена ПІД карткою домашнього завдання. Правила наповнення
  глосарію не змінились.
- Для вигляду списку доданий модифікатор `.chip.static` у
  `lesson-style.css` — той самий чіп, що й у словобанках вправ, але
  без `cursor:pointer`/hover-поведінки (він не клікабельний).

## Робочі правила (для мене, Claude)
- Два окремі Project: "генерація уроків" і "технічна підтримка
  сайту". Частина файлів спільна для обох (README.md, CHANGELOG.md,
  STYLE-BRIEF.md, lesson-template.html, lesson-style.css,
  lesson-interactions.js, lessons-data.js, glossary-data.js,
  modules-data.js) — зміна в одному місці не поширюється сама, тому
  **після кожної відповіді, де змінюється хоч один файл, я завжди
  нагадую, що куди покласти** (GitHub / Knowledge цього проєкту /
  Knowledge іншого проєкту, якщо файл спільний). Це не одноразова
  ввічливість, а стандартна частина відповіді.

## Інцидент: перезатертий Урок 18 (2026-09-25)
- Що сталося: урок 7-A-18 («Projekt: Schöne Familientraditionen») і
  12 його слів були в `lessons-data.js`/`glossary-data.js` на GitHub
  (коміт `1905c02`), але наступний коміт з уроком 7-B-1 (`21e3895`)
  залив версії обох файлів, де Уроку 18 вже не було. HTML уроку
  лишився на сайті, а запис у каталозі й слова зникли.
- Причина: B1 дописали в стару копію файлів з Project Knowledge
  (там Уроку 18 ще не було) і завантажили її на GitHub поверх новішої.
- Виправлено: взято версію з `1905c02` і B1 дописано в кінець (B1
  лишився останнім → має мітку "нове", модуль B розгортається в
  каталозі як актуальний).
- **Правило надалі:** `lessons-data.js` і `glossary-data.js` — файли,
  які ДОПИСУЮТЬСЯ, а не замінюються з нуля. Джерело правди — версія на
  GitHub, а копії в Knowledge обох проєктів можуть відставати. Перед
  генерацією нового уроку (і перед заливанням цих двох файлів на
  GitHub) — спершу поклади в Knowledge актуальну версію з GitHub.
  Claude у проєкті генерації, отримавши файл, має звірити, чи
  останній урок у ньому справді попередній до того, що генерується.

## Відкриті питання (ще не вирішено)
- (Приклад: сюди дописуй, що обговорюється, але ще не закрито)
- **Реєстрація/логін учнів** — обговорювали (2026-09-17), рішення не
  ухвалене, повертаємось пізніше. Мета: автоматична перевірка завдань,
  обмеження виконання одним разом, рейтинги студентів. Контекст:
  - Сайт зараз повністю статичний (GitHub Pages / Netlify Drop),
    бекенду й бази даних немає — логін вимагає зовнішнього сервісу
    аутентифікації (напр. Supabase Auth, Firebase Auth, Auth0), а не
    просто нового JS-файлу.
  - Для перевірки/обмеження завдань і рейтингів той самий сервіс має
    надавати ще й базу даних (Supabase — Postgres, Firebase —
    Firestore) — це одна зв'язка "логін + дані", не два окремі кроки.
  - Торкнеться `lesson-interactions.js` (логіка вправ треба буде
    зв'язати з бекендом) і, ймовірно, `lesson-template.html`.
  - GitHub Pages як хостинг сам по собі не заважає — аутентифікація й
    дані йдуть у зовнішній сервіс, Pages як віддавав статичні файли,
    так і віддає.
  - Рекомендований підхід, коли дійде черга: не проєктувати логін
    окремо, а одразу прикинути мінімальний повний ланцюжок
    (логін → збереження результату → перевірка "вже виконано" →
    рейтинг) як один інженерний етап, не по шматочку між баг-фіксами.
